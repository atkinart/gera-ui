
server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html index.htm;

  location / {
    try_files $uri /index.html;
  }

  # Proxy placeholder for future backend:
  # location /api/ {
  #   proxy_pass http://backend:8080;
  # }
}
